// @ts-check
import { defineConfig } from 'astro/config';

// User site (titille.github.io) → served at the domain root, so no `base` needed.
export default defineConfig({
  site: 'https://titille.github.io',
});
